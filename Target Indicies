class Solution:
    def targetIndices(self, nums, target: int):
        nums=sorted(nums)
        res=[]
        for i in range(len(nums)):
            if nums[i]==target:
                res.append(i)
        return res
